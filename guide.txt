1) Build->conf->local.conf

Cosas a editar:
	MACHINE ??= "zedboar-zynq7"

configuration defaults
	Agregar
	EXTRA_IMAGE_FEATURES ?= "dbg-pkgs src-pkgs dev-pkgs tools-sdk tools-debug"

Agregar al final
	DISTRO_FEATURES_append = "x11"	

2) Build->conf->bblayers.conf
	
